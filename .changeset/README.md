# Changesets

This folder is managed by [@changesets/cli](https://github.com/changesets/changesets).

To record a change for the next release, run `pnpm changeset` from the repo root and follow the prompts.
